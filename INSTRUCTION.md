# Django Todolist – Docker Compose Setup

This project runs Django Todolist application together with MySQL database using Docker Compose.

## 📦 Requirements

- Docker
- Docker Compose

Make sure Docker Desktop is installed and running.

---

## 🚀 How to Run the Application

### 1️⃣ Build and start containers

From the project root directory run:

```bash
docker-compose up --build
```

## docker-compose up --build
### 2️⃣ Stop and remove containers

To stop the running containers and remove them along with the default network, run:

```bash
docker-compose down